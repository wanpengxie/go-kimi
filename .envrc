# Shared machine-local credentials for go-kimi and dalek worktrees.
# Canonical secret store: ~/.config/go-kimi/.env
dotenv_if_exists "$HOME/.config/go-kimi/.env"

# Repo-local overrides for one checkout.
dotenv_if_exists .env.local
